#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

void main() {
	int ind;
	for (ind=15;ind>=4;cont--) {
		printf("A");
	}
/*	int i;
	for (i = 0; i<=10; i++) {
		printf("Valor de i = %d", i);
	}
	*/
	
/*	int num;
	printf("\n Insira um numero \n");
	scanf("%d", &num);
	while (num!=0) {
		num++;
		if (num >= 1 && num <= 10) {
		
		printf("O numero lido foi %d", num);
		}
	}*/
	
	//desenvolva um programa que leia 3 notas de 40 alunos, 
	//calcule e mostre a média aritmética e a situação de aprovação de cada aluno. Lembre-se que apenas a média igual ou acima de 7 aprova o aluno.
	
/*	int nota1, nota2, nota3;
	int media;
	int total_alunos;
	
	for (total_alunos = 1; total_alunos <= 40; total_alunos++) {
		if (media == 0) { // corrigir erro de msg
		printf("Entre com a primeira nota do aluno. \n");
		scanf("%d", &nota1);
		
		printf("Entre com a segunda nota do aluno. \n");
		scanf("%d", &nota2);
		
		printf("Entre com a terceira nota do aluno. \n");
		scanf("%d", &nota3);
		
		media = (nota1+nota2+nota3)/3;
		if (media>=7) {
				printf("Parabens, voce foi aprovado com a media %d", media);
		} else  {
				printf("Voce foi repovado, pois sua media foi inferior a 7 (%d)", media);
		}
	}
	} */

	
	
}
